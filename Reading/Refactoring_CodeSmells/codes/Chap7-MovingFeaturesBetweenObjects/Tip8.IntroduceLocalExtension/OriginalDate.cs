namespace Tip8.IntroduceLocalExtension
{
    internal class OriginalDate
    {
        private readonly DateTime _date;

        public OriginalDate(DateTime date)
        {
            _date = date;
        }

        // Extra task to do: add one method to check if a date is holiday
    }
}
