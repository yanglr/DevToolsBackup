**Description**:

`TrackingInformation`类存储了一次物流运输(shipment)的若干跟踪信息（tracking information）。
它作为Shipment(物流)类的一部分被使用。

Class `TrackingInformation` holds a couple of pieces of tracking information for a shipment.